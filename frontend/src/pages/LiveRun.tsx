import { useEffect, useState } from "react"; import { api } from "../api/client"; import type { WorkflowEvent } from "../types"; import { StatusPill } from "../components/StatusPill";
export function LiveRun({ batchId }: { batchId: string }) { const [events, setEvents] = useState<WorkflowEvent[]>([]); const [connection, setConnection] = useState("Connecting"); useEffect(() => { if (!batchId) return; const stream = new EventSource(api.eventUrl(batchId)); stream.onopen = () => setConnection("Live"); const add = (event: Event) => setEvents((current) => [...current, JSON.parse((event as MessageEvent).data)]); ["node_started", "node_completed", "progress", "workflow_paused", "workflow_resumed", "record_validated", "push_result", "rollback", "workflow_failed"].forEach((type) => stream.addEventListener(type, add)); stream.onerror = () => setConnection("Reconnecting"); return () => stream.close(); }, [batchId]); const paused = events.some((event) => event.event_type === "workflow_paused") && !events.some((event) => event.event_type === "workflow_resumed"); return <section><div className="section-heading"><div><p className="eyebrow">Workflow activity</p><h2>Live run</h2></div><StatusPill value={paused ? "PAUSED" : connection.toUpperCase()} /></div>{paused && <div className="pause-banner" role="status"><strong>Human decision required</strong><span>The workflow is safely paused. Review the open escalation to continue.</span></div>}{!batchId ? <div className="empty"><h3>No active migration</h3><p>Create a migration to see stage progress and safe action summaries.</p></div> : <ol className="timeline">{events.map((event) => <li key={event.event_id}><span>{new Date(event.timestamp).toLocaleTimeString()}</span><div><strong>{event.event_type.replaceAll("_", " ")}</strong><pre>{JSON.stringify(event.payload)}</pre></div></li>)}</ol>}</section>; }

